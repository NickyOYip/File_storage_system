<!DOCTYPE html>
<html>
<head>
    <title>Delete File - File Storage System</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .container { max-width: 600px; margin: 0 auto; }
        .warning { color: #f44336; margin: 20px 0; }
        .button { padding: 10px 15px; text-decoration: none; margin-right: 10px; }
        .delete { background-color: #f44336; color: white; border: none; cursor: pointer; }
        .back { background-color: #666; color: white; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Delete File</h1>
        <div class="warning">
            <p>Are you sure you want to delete this file?</p>
            <p><strong>File Name:</strong> <%= file.originalName %></p>
        </div>
        <form action="/files/<%= file._id %>/delete" method="POST">
            <button type="submit" class="button delete">Confirm Delete</button>
            <a href="/user/dashboard" class="button back">Cancel</a>
        </form>
    </div>
</body>
</html>